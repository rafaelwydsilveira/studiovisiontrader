{
  "buildCommand": "cd ../.. && npm install && cd apps/web && npx prisma generate && npx next build",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && npm install --ignore-scripts",
  "framework": "nextjs"
}
